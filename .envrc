# Home Manager supplies nix-direnv; never download shell code during activation.
# Watch imported modules AND directories, so additions/deletions invalidate the cache.
# Bash globstar is available in direnv's Nix-provided Bash.
shopt -s globstar nullglob
watch_file modules modules/**/ modules/**/*.nix tests tests/**/*.nix
use flake . --impure
